<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!--
        Required for NIP-55 external signer detection on Android 11+ (API 30+).
        Without this block PackageManager.queryIntentActivities returns an empty
        list even when Amber (com.greenart7c3.nostrsigner) is installed.
        Each signer's intentScheme here MUST match KNOWN_NOSTR_SIGNERS in
        app/src/main/java/com/highlighter/app/nip55/ExternalSignerWire.kt.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="nostrsigner" />
        </intent>
        <package android:name="com.greenart7c3.nostrsigner" />
    </queries>

    <application
        android:name=".HighlighterApplication"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Highlighter">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Highlighter">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="nip46"
                    android:scheme="highlighter" />
            </intent-filter>
            <!--
                Verified App Link for canonical highlight share URLs minted by the
                core (https://beta.highlighter.com/highlight/{nevent}; see
                app/core/src/share_links.rs). autoVerify requires a matching
                /.well-known/assetlinks.json hosted on beta.highlighter.com that
                lists this app's package + signing-cert SHA-256 — that is a
                SERVER-SIDE prerequisite outside this repo.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:scheme="http" />
                <data android:host="beta.highlighter.com" />
                <data android:pathPrefix="/highlight/" />
            </intent-filter>
            <!--
                Custom-scheme fallback for the same highlight links, so the app
                still resolves them even before App Link verification succeeds
                (or on devices/launchers that bypass it).
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="highlight"
                    android:scheme="highlighter" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
    </application>
</manifest>
